<section class="section">
  <div class="section__inner">
    <article class="article">
      <%
      let feats = discographyData.feats,
        reverseFeats = feats.reverse();
      %>
      <figure class="article__hero">
        <img class="article__hero-image" src="<%- reverseFeats[page.index].image %>" alt="<%- reverseFeats[page.index].alt %>">
      </figure>
      <div class="article__inner">
        <h3 class="article__title"><%- reverseFeats[page.index].title %></h3>
        <div class="article__body">
          <ul class="article__caption-list">
            <li class="article__caption-item">作品種別 : <%- reverseFeats[page.index].type %></li>
            <li class="article__caption-item">発売日 : <%- reverseFeats[page.index].release %></li>
            <% if (!reverseFeats[page.index].price.trim() === false) { %>
            <li class="article__caption-item">価格 : <%- reverseFeats[page.index].price %></li>
            <% } %>
            <li class="article__caption-item">視聴：ご視聴・ご購入は<a href="<%- reverseFeats[page.index].link %>" target="_blank" rel="noopener noreferrer">こちら</a>から</li>
          </ul>
          <h3 class="article__caption-heading">Song contents</h3>
          <ol class="article__caption-list article__caption-list--song">
            <% reverseFeats[page.index].songs.forEach(function(song) { %>
            <li class="article__caption-item">
              <%- song %>
            </li>
            <% }); %>
          </ol>
          <% if (!reverseFeats[page.index].bonusTracks[0].trim() === false) { %>
          <h3 class="article__caption-heading">Bonus track</h3>
          <ol class="article__caption-list article__caption-list--song">
            <% reverseFeats[page.index].bonusTracks.forEach(function(bonusTrack) { %>
            <li class="article__caption-item">
              <%- bonusTrack %>
            </li>
            <% }); %>
          </ol>
          <% } %>
        </div>
      </div>
    </article>
    <div class="button__wrap">
      <a class="button" href="/discography/">
        <span class="button__text">Back</span>
      </a>
    </div>
  </div>
</section>
