<section class="section">
  <div class="section__inner">
    <article class="article">
      <%
      let posts = newsData.posts,
        reversePosts = posts.reverse();
      %>
      <% if (reversePosts[page.index].hero.image !== '') { %>
      <figure class="article__hero">
        <img class="article__hero-image" src="<%- reversePosts[page.index].hero.image %>" alt="<%- reversePosts[page.index].hero.alt %>">
      </figure>
      <% } %>
      <div class="article__inner">
        <h3 class="article__title"><%- reversePosts[page.index].title %></h3>
        <div class="article__body">
          <% reversePosts[page.index].leads.forEach(lead => { %>
          <p class="text"><%- lead %></p>
          <% }); %>
          <% if (reversePosts[page.index].photos.length !== 0) { %>
          <% reversePosts[page.index].photos.forEach(photo => { %>
          <figure class="article__image-wrap">
            <img class="article__image" src="<%- photo.image %>" alt="<%- photo.alt %>">
          </figure>
          <% }); %>
          <% } %>
        </div>
      </div>
    </article>
    <div class="button__wrap">
      <a class="button" href="/news/">
        <span class="button__text">Back</span>
      </a>
    </div>
  </div>
</section>
