<!doctype html>
<html lang="ja">
  <head>
    <!-- prettier-ignore -->
    <%_
      const localmeta = {
        title: 'NEWS | SUSHIBOYS',
        description: 'SUSHIBOYSニュース。ライブ出演、リリース情報、メディア掲載等の最新情報をご覧いただけます。',
        url: 'https://sushiboys350.com/news/',
        pageid: 'news',
        path: '../../'
      };
      const localogp = {
        title: localmeta.title,
        description: localmeta.description,
        url: localmeta.url
      };
      const localfile = {
        css: '/assets/css/news.css',
        js: '/assets/js/news.js'
      };
    _%>
    <%- include(localmeta.path + '_components/head', {meta: localmeta, ogp: localogp, file: localfile}); _%>
    <%- include(localmeta.path + '_data/news'); _%>
    <%_ newsData = news(); _%>
  </head>
  <body>
    <%- include(localmeta.path + '_components/googletagmanager'); _%>

    <div class="wrapper">
      <!-- prettier-ignore -->
      <%- include(localmeta.path + `_components/global-navigation`, {meta: localmeta}); _%>

      <main class="main">
        <!-- prettier-ignore -->
        <%- include(localmeta.path + '_components/header', {meta: localmeta}); _%>

        <div>
          <!-- prettier-ignore -->
          <%- include(localmeta.path + '_components/hero', {meta: localmeta}); _%>

          <div class="content">
            <!-- prettier-ignore -->
            <%- include(localmeta.path + '_components/newsList', {meta: localmeta, newsData: newsData}); _%>
          </div>
        </div>

        <!-- prettier-ignore -->
        <%- include(localmeta.path + `_components/toggle`); _%>

        <%- include(localmeta.path + `_components/footer`); _%>
      </main>
    </div>
  </body>
</html>
