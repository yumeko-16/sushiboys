<!doctype html>
<html lang="ja">
  <head>
    <!-- prettier-ignore -->
    <%_
      const localmeta = {
        title: `BIOGRAPHY | SUSHIBOYS`,
        description: `SUSHIBOYSの生態系。`,
        url: `https://sushiboys350.com/biography/`,
        pageid: `biography`,
        path: '../../'
      };

      const localogp = {
        title: localmeta.title,
        description: localmeta.description,
        url: localmeta.url
      };

      const localfile = {
        css: `/assets/css/biography.css`,
        js: `/assets/js/biography.js`
      };
    _%>
    <%- include(localmeta.path + `_components/head`, {meta: localmeta, ogp: localogp, file: localfile}); _%>
  </head>
  <body>
    <%- include(localmeta.path + `_components/googletagmanager`); _%>

    <div class="wrapper">
      <!-- prettier-ignore -->
      <%- include(localmeta.path + `_components/global-navigation`, {meta: localmeta}); _%>

      <main class="main">
        <!-- prettier-ignore -->
        <%- include(localmeta.path + `_components/header`, {meta: localmeta}); _%>

        <div>
          <!-- prettier-ignore -->
          <%- include(localmeta.path + `_components/hero`, {meta: localmeta}); _%>

          <div class="content">
            <section class="section">
              <div class="section__inner">
                <%- include(localmeta.path + `_components/biography`); _%>
              </div>
            </section>
          </div>
        </div>

        <!-- prettier-ignore -->
        <%- include(localmeta.path + `_components/toggle`); _%>

        <%- include(localmeta.path + `_components/footer`); _%>
      </main>
    </div>
  </body>
</html>
